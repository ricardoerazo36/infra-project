apiVersion: apps/v1
kind: Deployment
metadata:
  name: news-analyzer
  namespace: news-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: news-analyzer
  template:
    metadata:
      labels:
        app: news-analyzer
    spec:
      containers:
        - name: analyzer
          image: proyecto_analyzer:latest
          imagePullPolicy: Never
          env:
            - name: SLEEP_INTERVAL
              value: "1800"
          volumeMounts:
            - name: shared-data
              mountPath: /app/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: shared-data
          persistentVolumeClaim:
            claimName: news-data-pvc